# memcached-docker
memcached-docker

### usage
run by docker 
```
docker run -p 11211:11211 --name memcached -d memcached:alpine memcached -m 64
```

run by docker-compose 
```
docker-compose up
```

